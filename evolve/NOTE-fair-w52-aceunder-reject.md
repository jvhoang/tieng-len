# W52 com_aceunder — REJECT (dual-null)

**Target:** B `20370505@1` s4 AD→QD (full-policy force WIN on seqhires path).

**Result:** Fingerprint lever fires (firstdiff AD→QD) but **holdout seat1 still L** vs freeze v95 — same as base. Force-once ≠ full hard policy (pairkeep class).

Loose bands also thrash BR at s2 (9D→7D).

**Keep stack at p_w51_ex_seqhires (B28 vs v95 id).** SoftN forbidden.
